This repository demonstrates how to deploy a simple Three.js content using Vite to build; and GitHub Actions to deploy to GitHub Pages.

# Deployment Steps

Change `vite.config.js` to point to your repository by updating the `base` property.


